# Data source: yahoo (recommended default) | demo | custom
# The UI also defaults to Yahoo live if this file is missing.
VITE_DATA_SOURCE=yahoo

# Optional custom chain API. Used automatically if VITE_DATA_SOURCE=custom.
# GET {url}?symbol=SPY
# VITE_OPTIONS_API_URL=https://your-api.example/options
